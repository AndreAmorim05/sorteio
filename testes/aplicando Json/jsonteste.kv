<Manager>:
    Menu:
    Tela:
    Historico:

<Menu>:
    name: 'menu'
    FloatLayout:
        BoxLayout:
            orientation: 'vertical'
            pos_hint: {'center_x': .5, 'center_y': .5}
            size_hint: .3, .4
            Button:
                text: 'Tela1'
                on_release: root.manager.current = 'tela1'
            Button:
                text:'Histórico'
                on_release: root.manager.current = 'historico'


<Tela>:
    name: 'tela1'
    FloatLayout:
        Button:
            text: 'Voltar'
            pos_hint: {'right': 1, 'top': 1}
            size_hint: .15, .1
            on_release: root.manager.current = 'menu'

        Button:
            text: 'Sortear'
            pos_hint: {'center_x': .5, 'center_y': .5}
            size_hint: .25, .15
            on_release: root.save_json()

<Historico>:
    name: 'historico'
    FloatLayout:
        Button:
            text: 'Voltar'
            pos_hint: {'right': 1, 'top': 1}
            size_hint: .15, .1
            on_release: root.manager.current = 'menu'